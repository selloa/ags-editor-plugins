# Game States (AOTT)

Editor plugin tuned for **Audit of the Tentacle**: scans `episode_irs_audit.asc` for `EpisodeIRS_GameStartInit` in addition to generic `game_start` init values.

Use the generic [`AGS.Plugin.GameStates`](../AGS.Plugin.GameStates/) plugin for other projects.
